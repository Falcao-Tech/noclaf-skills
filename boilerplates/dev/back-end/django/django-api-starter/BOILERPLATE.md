---
title: API Django (DRF + JWT)
description: API REST em Django 4.2 + DRF com JWT rotativo, envelope padronizado, S3, soft delete e UUID nos models.
repo: https://github.com/Falcao-Tech/boilerplate-api-django
---

Escolha quando o projeto novo é uma API interna que já nasce com autenticação,
documentação (drf-spectacular) e as integrações Noclaf ligadas.

Repo **privado** — clonar exige acesso à org Falcao-Tech (o `gh auth login` do worker
já resolve). O `--apply` falha no clone pra quem estiver de fora.
